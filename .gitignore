# Personal data — never commit. Lives only in the browser's localStorage.
myseed.json
*.local.json

# OS
.DS_Store

# Cloudflare Worker local
worker/.wrangler/
worker/node_modules/
worker/.dev.vars

# Node server deps / local env
server/node_modules/
server/.env
node_modules/
